# This file is licensed under the Apache License v2.0 with LLVM Exceptions.
# See https://llvm.org/LICENSE.txt for license information.
# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

module(
    name = "llvm-project",
    version = "20.0.0",
)

# Core dependencies from Bazel Central Registry
bazel_dep(name = "bazel_skylib", version = "1.7.1")
bazel_dep(name = "rules_cc", version = "0.0.17")
bazel_dep(name = "rules_python", version = "1.0.0")
bazel_dep(name = "rules_foreign_cc", version = "0.11.1")
bazel_dep(name = "platforms", version = "0.0.10")

# Apple support
bazel_dep(name = "apple_support", version = "1.15.1", repo_name = "build_bazel_apple_support")

# Shell rules for tests
bazel_dep(name = "rules_shell", version = "0.6.1")

# Register Python toolchain
python = use_extension("@rules_python//python/extensions:python.bzl", "python")
python.toolchain(
    python_version = "3.12",
)

# Note: The llvm-raw local repository and llvm_configure are still handled
# through WORKSPACE for compatibility. When bzlmod is enabled alongside
# --enable_workspace, both MODULE.bazel and WORKSPACE are processed.
# This allows for a gradual migration to bzlmod.
